{
  "name": "dewey-api",
  "version": "1.0.0",
  "description": "The API for the Dewey App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node index.js",
    "migrate": "npx sequelize-cli db:migrate",
    "create model": "npx sequelize-cli model:generate"
  },
  "author": "Nathanael Metke",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mariadb": "^3.1.0",
    "sequelize": "^6.29.3"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.0"
  }
}
